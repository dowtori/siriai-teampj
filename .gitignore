# ── 개인정보 ──────────────────────────────────────────
# 실명 · 연락처 · 자택 주소가 들어 있는 원본. 절대 올리지 않는다.
# 배포에는 가명 처리된 prototype/data/db.json 만 올라간다.
prototype/data/db.local.json

# ── 운영 원본 ─────────────────────────────────────────
# 거래처별 매출 · 순이익과 협업 인플루언서 명단이 그대로 든 원본.
# 마이그레이션 입력일 뿐이라 레포에는 올리지 않는다.
prototype/scripts/master.xlsx
prototype/scripts/creators.csv

# ── 의존성 · 빌드 산출물 ──────────────────────────────
node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# ── 환경 · 배포 ───────────────────────────────────────
.env*
!.env.example
.vercel

# ── 로컬 설정 ─────────────────────────────────────────
.claude/settings.local.json

# ── 기타 ──────────────────────────────────────────────
.DS_Store
Thumbs.db
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
